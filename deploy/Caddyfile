# TLS termination for psyrag serve — Caddy provisions certificates
# automatically. Pair with `psyrag serve --addr 127.0.0.1:8080 --token ...`.
psyrag.example.com {
    reverse_proxy 127.0.0.1:8080
    request_body {
        max_size 32MB
    }
}
